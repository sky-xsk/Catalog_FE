import * as types from '../mutation-types'

import shop from '../../api/shop'

// initial state
const state = {
   
    
}

// getters
const getters = {
    
}

// actions
const actions = {
    
}

// mutations
const mutations = {
    

}

export default {
    state,
    getters,
    actions,
    mutations
}